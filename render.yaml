services:
  - type: web
    name: f1-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server --workers 2 --timeout 60
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
