services:
  - type: web
    name: f1-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:application --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
